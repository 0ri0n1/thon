# Pineapple Recon Report — {{ run.engagement }}

**Generated (UTC):** {{ generated_utc }}
**Schema:** `pineapple_recon.manifest/v1`

## Run metadata

| Field | Value |
|---|---|
| Run ID | `{{ run.run_id }}` |
| Engagement | `{{ run.engagement }}` |
| Pineapple host | `{{ run.host }}` |
| User | `{{ run.user }}` |
| Interface | `{{ run.iface }}` |
| Band | `{{ run.band }}` |
| Duration (configured) | {{ run.duration }}s |
| Started (UTC) | {{ run.started_utc }} |
| Ended (UTC) | {{ run.ended_utc or 'n/a' }} |
| Status | **{{ run.status }}**{% if run.error %} — {{ run.error }}{% endif %} |

## Summary

- **Access points observed:** {{ ap_count }}
- **Hidden SSIDs:** {{ hidden_count }}
- **2.4 GHz APs:** {{ ch_2g }}
- **5 GHz APs:** {{ ch_5g }}

### Encryption breakdown

| Encryption | Count |
|---|---:|
{% for enc, n in enc_counts.items() %}
| {{ enc }} | {{ n }} |
{% endfor %}

## Interesting findings

{% if flagged.open %}
### Open networks ({{ flagged.open | length }})

| SSID | BSSID | Ch | Signal |
|---|---|---:|---:|
{% for a in flagged.open %}
| {{ a.essid }} | `{{ a.bssid }}` | {{ a.channel }} | {{ a.signal_dbm }} dBm |
{% endfor %}
{% endif %}
{% if flagged.wep %}
### WEP networks ({{ flagged.wep | length }})

| SSID | BSSID | Ch | Signal |
|---|---|---:|---:|
{% for a in flagged.wep %}
| {{ a.essid }} | `{{ a.bssid }}` | {{ a.channel }} | {{ a.signal_dbm }} dBm |
{% endfor %}
{% endif %}
{% if flagged.enterprise %}
### WPA2-Enterprise networks ({{ flagged.enterprise | length }})

| SSID | BSSID | Ch | Signal |
|---|---|---:|---:|
{% for a in flagged.enterprise %}
| {{ a.essid }} | `{{ a.bssid }}` | {{ a.channel }} | {{ a.signal_dbm }} dBm |
{% endfor %}
{% endif %}
{% if flagged.hidden %}
### Hidden SSIDs ({{ flagged.hidden | length }})

| BSSID | Ch | Encryption | Signal |
|---|---:|---|---:|
{% for a in flagged.hidden %}
| `{{ a.bssid }}` | {{ a.channel }} | {{ a.encryption }} | {{ a.signal_dbm }} dBm |
{% endfor %}
{% endif %}
{% if not (flagged.open or flagged.wep or flagged.enterprise or flagged.hidden) %}
_No flagged findings._
{% endif %}

## All access points (sorted by signal)

| SSID | BSSID | Ch | Encryption | Signal | Beacons | Vendor |
|---|---|---:|---|---:|---:|---|
{% for a in aps %}
| {{ a.essid }} | `{{ a.bssid }}` | {{ a.channel }} | {{ a.encryption }} | {{ a.signal_dbm }} dBm | {{ a.beacons }} | {{ a.vendor }} |
{% endfor %}

## Artifacts

Raw artifacts in `./raw/` relative to this report.

| File | Purpose |
|---|---|
| `raw/scan.csv` | airodump-ng full CSV (APs + clients) |
| `raw/scan.pcap` | raw beacon/probe capture |
| `raw/iw-scan.txt` | one-shot `iw dev <iface> scan` cross-reference |
| `raw/iface_info.txt` | interface state at run time |
| `raw/manifest.json` | normalized AP data consumed by this report |
| `raw/sha256sums.txt` | integrity hashes |
| `run.log` | collection script stdout/stderr |

### Integrity (SHA256)

```
{{ integrity }}
```

## Run log (last 20 lines)

```
{{ log_tail }}
```

---

_Report generated by `pineapple_recon` skill. Deterministic template — no agent narration applied._
